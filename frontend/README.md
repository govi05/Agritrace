# AgriTrace Frontend

Built with React.js. Three role-gated portals:
- Farmer Dashboard
- Customer QR Scanner  
- Admin Console

## Setup
```bash
npx create-react-app .
npm install axios react-router-dom
npm start
```

Dashboards are in `src/pages/`.
Connects to backend API at `http://localhost:3000`.
